[build]
  publish = "."
  command = "echo 'No build needed for static site'"

[dev]
  command = "npx http-server . -p 3000 -c-1"
  port = 3000
  targetPort = 3000
  publish = "."
  autoLaunch = false
